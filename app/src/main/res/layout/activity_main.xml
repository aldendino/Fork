<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"

    tools:context=".MainActivity" >

    <!--
    <Button
        android:id="@+id/button2"
        android:layout_marginTop="@dimen/buttonMargin"
        android:layout_marginRight="@dimen/buttonMargin"
        android:layout_marginLeft="@dimen/buttonMargin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        style="?android:attr/buttonStyleSmall"
        android:text="@string/add_button" />
        -->

    <TextView
        android:id="@+id/textViewBanner"
        android:background="@color/banner_colour"
        android:singleLine="true"
        android:ellipsize="middle"
        android:paddingTop="@dimen/bannerPadding"
        android:paddingBottom="@dimen/bannerPadding"
        android:paddingLeft="@dimen/bannerPadding"
        android:paddingRight="@dimen/bannerPadding"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <ListView
        android:id="@+id/listView1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" >

        <requestFocus />

    </ListView>

</LinearLayout>
